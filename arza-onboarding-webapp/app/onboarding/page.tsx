export const dynamic = 'force-dynamic';
export const dynamic = 'force-dynamic';

// Importar el contenido original de la página
import OnboardingContent from './onboarding-content';

export default function OnboardingPage() {
  return <OnboardingContent />;
}
